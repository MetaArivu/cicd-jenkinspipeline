pipeline {
    agent any
    tools { nodejs "nodejs" }  
    stages {
        stage('Build') {
            steps {
                echo 'Build the App'
                sh 'node -v'
                sh 'npm -i'
            }
        }
        stage('Test') {
            steps {
                echo 'Test the App'
            }
        }   
        stage('Staging') {
            steps {
                echo 'Deploy the App in Staging'

            }
        }
    }
}
